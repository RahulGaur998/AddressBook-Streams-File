// package com.addressbook;

// public Set newSet = new HashSet<>();
// newSet.add(findDuplicateBySetAdd(arrli, firstName));
// if (!newSet.contains(firstName)) {
// arrli.add(new Contact(firstName, lastName, address, city, state, zip,
// phoneNumber, emailId));
// } else {
// System.out.println("Duplicate contact found: " + newSet.toString());
// addContact(s);
// }
// System.out.println(newSet); junk {

// }
